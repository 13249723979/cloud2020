server:
  port: 3377
spring:
  application:
    name: nacous-config
  cloud:
    nacos:
      nacos:
        server-addr: localhost:8848
    config:
      server-addr: localhost:8848
      file-extension: yaml